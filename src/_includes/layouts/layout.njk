<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>{{ pageTitle }}</title>
    <link href="/css/main.css" rel="stylesheet">
  </head>
  <body>
    <h1>{{ pageTitle }}</h1>
    {{ content | safe }}
  </body>
</html>